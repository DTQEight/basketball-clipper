# Python
__pycache__/
*.py[cod]
*.egg-info/
*.so
.Python
venv/
env/
ENV/

# 密钥（不要上传）
.env
*.key

# 训练数据（太大，不上传）
train_data/
train_data_manual/
runs/
demo_output/

# training/ 大体积中间数据（帧缓存/特征/裁剪块，可由 training/ 脚本再生）
training/frames_b/
training/frames_b_feat/
training/frames_b_feat_simclr/
training/frames_c/
training/flow_b/
training/feat_c_ball/
training/feat_c_glob/
training/feat_flow/
training/feat_traj/
training/tracks_c/
training/traj_c/
training/vm_feat/
training/clips/
training/yolo_data/
training/crops_all.npy
training/features*.jsonl

# 视频文件（太大）
*.mp4
*.avi
*.mov
*.mkv
*.ts
*.flv

# 权重文件（太大，用 release 上传）
weights/*.pt
*.pt

# 缓存目录（项目根目录下的 cache/，内含 detection_history.json、preview 片段、输出缓存）
cache/
.cache/
*.cache

# 临时工作目录（本地曾把项目放在 basketball-clipper/ 子目录下，远端是扁平结构，应忽略）
basketball-clipper/

# IDE
.vscode/
.idea/
*.swp
*.swo

# 系统
.DS_Store
Thumbs.db
desktop.ini

# 日志
*.log
server.log
